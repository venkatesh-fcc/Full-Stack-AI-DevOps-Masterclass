name: Second CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        env:
          GREETING: "Hello from the second workflow!"
        run: echo "Hi there! $GREETING"
